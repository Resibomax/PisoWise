name: Format Check

on:
  push:
    branches:
      - '**'
    paths:
      - 'Apps/pisowise-fe/**'
  pull_request:
    branches:
      - '**'

jobs:
  format:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: Apps/pisowise-fe/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: Apps/pisowise-fe

      - name: Check formatting
        run: npm run format:check
        working-directory: Apps/pisowise-fe
